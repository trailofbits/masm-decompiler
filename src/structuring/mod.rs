//! Structuring passes that will eventually reconstruct high-level control flow.
//!
//! Currently a placeholder to keep the module graph intact.

use crate::cfg::Cfg;

pub fn structure(_cfg: Cfg) -> Vec<String> {
    Vec::new()
}
